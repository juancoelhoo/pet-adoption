interface RegisterData {
    username: string;
    email: string;
    password: string;
  }
  
  export const validateRegister = (data: RegisterData): boolean => {
    // Implementar validação
    return true;
  }
  